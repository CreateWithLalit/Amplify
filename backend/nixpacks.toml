[phases.setup]
nixPkgs = ["nodejs", "python3", "ffmpeg"]

[phases.build]
cmds = ["npm install", "curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o yt-dlp", "chmod a+rx yt-dlp"]

[start]
cmd = "node index.js"
